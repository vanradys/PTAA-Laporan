kind = "web"
previewPath = "/"
title = "Laporan Harian"
version = "1.0.0"
id = "artifacts/laporan-harian"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23822

[services.development]
run = "pnpm --filter @workspace/laporan-harian run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/laporan-harian", "run", "build" ]
publicDir = "artifacts/laporan-harian/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23822"
BASE_PATH = "/"
